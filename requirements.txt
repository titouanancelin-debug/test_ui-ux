# Dépendances du bot de scalping
pandas>=2.0
numpy>=1.24
requests>=2.28

# Optionnel : chargement automatique du fichier .env
python-dotenv>=1.0

# Optionnel : visualisation des backtests (graphiques)
matplotlib>=3.7
